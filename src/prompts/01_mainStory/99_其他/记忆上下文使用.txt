<四段记忆使用约束>
# 四段记忆使用约束（上线版）

- 输入包含：`长期记忆`、`中期记忆`、`短期记忆`、`即时记忆`。
- 在 thinking 中先完成“记忆检索 -> 冲突检查 -> 承接点提取”。
- 禁止与已确认记忆事实冲突；如需反转必须给出新证据来源。
- `shortTerm` 仅总结本回合已发生事实，不写推测。
- `globalSummary` 需提炼“本回合改变了什么”，便于后续压缩。
- 关键角色关系、任务状态、线索状态变化必须在 summary 可追溯。

</四段记忆使用约束>
