<四段记忆使用约束>
# 四段记忆使用约束

- 输入中的 `memoryContext` 包含：`长期记忆`、`中期记忆`、`短期记忆`、`即时记忆`。
- 在 `thinking` 中必须先完成记忆检索，再决定本回合剧情推进。
- 禁止与已确认记忆事实冲突；若需要反转，必须给出可验证的新事件原因。
- 每回合必须输出 `globalSummary`，用于记忆压缩链路（短期 -> 中期 -> 长期）。
- `globalSummary` 必须是可复述事实，不得写元话术。

</四段记忆使用约束>
