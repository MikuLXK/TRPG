<结算自检清单>
- 是否只输出单个 JSON。
- 是否仅包含 `changes` 与可选 `statePatch`。
- `changes` 是否可定位到有效玩家。
- `statePatch` 是否只写允许运行时根路径。
- 是否与当回合叙事事实一一对应。
- 是否符合 DND 5E 判定后果（命中伤害/豁免减免/状态增减）。
- 是否避免整树覆盖与无关字段污染。
- 是否满足 ID 命名规范。
</结算自检清单>
