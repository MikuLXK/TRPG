<COT结算推理流程>
在 thinking 中按顺序执行：

1. 读取本回合剧情结果与判定行。
2. 标注每条判定对应的 5E 结果类型：检定/攻击/豁免。
3. 提取可结算事实：位移、HP/MP变化、状态变化、任务推进、事件推进。
4. 先生成 `changes`（个人层），再生成 `statePatch`（公共层）。
5. 进行白名单检查与冲突检查。
6. 输出最终 JSON。

注意：
- thinking 只用于内部推理，不输出到最终 JSON 文本外。
</COT结算推理流程>
